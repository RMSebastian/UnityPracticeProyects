using UnityEngine;

public class MachineryManager : MonoBehaviour
{
    /*
    	- comienzan bloqueadas exepto por 1
	- Al pasar de dia se desbloque 1
	- Pueden tener una maquina para generar Recursos
	- Al clickear la mina sin maquina colocada se abre el panel de colocacion de maquina en el cual muestra las maquinas que se pueden colocar en esa mina en base al color del Recurso que genera. (Ejemplo: si la mina genera Recurso de color verde, solo se pueden colocar maquinas que extran recursos de color verde)
	- [Opcional] Al terminar el dia el recurso puede pasar a ser mas valioso
	- [Opcional] Se pueden comprar el desbloqueado
    */
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
